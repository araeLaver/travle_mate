# Logstash Configuration for Fryndo

http.host: "0.0.0.0"
path.config: /usr/share/logstash/pipeline

# Monitoring
xpack.monitoring.enabled: false

# Pipeline settings
pipeline.workers: 2
pipeline.batch.size: 125
pipeline.batch.delay: 50

# Logging
log.level: info
log.format: plain
