# ===========================================
# TravelMate Backend 환경변수 설정
# ===========================================

# JWT Configuration (필수!)
# - 최소 32자 이상의 랜덤 문자열 사용
# - 생성 예시: openssl rand -base64 32
JWT_SECRET=your-secure-random-key-at-least-32-characters-long
JWT_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=604800000

# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/travelmate
DB_USERNAME=travelmate_user
DB_PASSWORD=your-secure-password

# Spring Profile (dev | prod | test)
SPRING_PROFILES_ACTIVE=dev

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost

# Server Configuration
SERVER_PORT=8080

# Email Configuration (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=noreply@travelmate.app

# File Upload
FILE_UPLOAD_PATH=./uploads/
MAX_FILE_SIZE=10485760

# Redis (운영환경)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Frontend URL (이메일 링크용)
FRONTEND_URL=http://localhost:3000
