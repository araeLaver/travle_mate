server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json,application/xml
  http2:
    enabled: true
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,livenessState,readinessState,env,loggers
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
      show-components: always
      probes:
        enabled: true
    metrics:
      enabled: true
    prometheus:
      enabled: true
  health:
    circuitbreakers:
      enabled: true
    db:
      enabled: true
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}
      environment: ${spring.profiles.active:unknown}
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.90, 0.95, 0.99

spring:
  application:
    name: travel-mate-backend

# OpenAPI/Swagger 설정
springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true
    doc-expansion: none
  show-actuator: false
  packages-to-scan: com.travelmate.controller
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

  lifecycle:
    timeout-per-shutdown-phase: 30s

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

---
# 개발 환경 설정
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/travelmate}
    username: ${DB_USERNAME:travelmate_user}
    # SECURITY: DB_PASSWORD 환경변수 필수 설정
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000

  sql:
    init:
      mode: always
      schema-locations: classpath:schema-init.sql
      continue-on-error: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true
        default_schema: travelmate
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

logging:
  level:
    com.travelmate: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

app:
  jwt:
    # SECURITY: JWT_SECRET 환경변수 필수 설정 (최소 32자, Base64 권장)
    # 생성 명령어: openssl rand -base64 32
    secret: ${JWT_SECRET:}
    expiration: ${JWT_EXPIRATION:86400000}  # 24 hours
    refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}  # 7 days

  mail:
    enabled: ${MAIL_ENABLED:false}  # 개발환경에서는 비활성화

  redis:
    enabled: true  # 개발환경에서는 Redis 활성화

  frontend:
    url: ${FRONTEND_URL:http://localhost:3000}

  file:
    upload:
      path: ./uploads/
      max-size: 10485760  # 10MB in bytes

  cors:
    allowed-origins: http://localhost:3000,http://localhost:8081

  security:
    rate-limit:
      enabled: false  # 개발환경에서는 비활성화
      requests-per-minute: 60
      auth-requests-per-minute: 10
    xss:
      enabled: true
    audit:
      enabled: true

# Sentry 설정 (개발)
sentry:
  enabled: ${SENTRY_ENABLED:false}
  dsn: ${SENTRY_DSN:}
  environment: development
  traces-sample-rate: 0.1
  logging:
    minimum-event-level: warn
    minimum-breadcrumb-level: info

# 블록체인 설정 (Polygon)
blockchain:
  enabled: ${BLOCKCHAIN_ENABLED:false}
  polygon:
    rpc-url: ${POLYGON_RPC_URL:https://polygon-amoy.g.alchemy.com/v2/demo}
    chain-id: ${POLYGON_CHAIN_ID:80002}
    contract-address: ${NFT_CONTRACT_ADDRESS:}
    private-key: ${BLOCKCHAIN_PRIVATE_KEY:}

# IPFS 설정
ipfs:
  enabled: ${IPFS_ENABLED:false}
  gateway-url: ${IPFS_GATEWAY_URL:https://ipfs.io/ipfs/}
  api-url: ${IPFS_API_URL:}
  api-key: ${IPFS_API_KEY:}

# AWS 설정 (개발)
aws:
  region: ${AWS_REGION:ap-northeast-2}
  access-key-id: ${AWS_ACCESS_KEY_ID:}
  secret-access-key: ${AWS_SECRET_ACCESS_KEY:}
  use-default-credentials: true
  cloudfront:
    distribution-id: ${CLOUDFRONT_DISTRIBUTION_ID:}
    key-pair-id: ${CLOUDFRONT_KEY_PAIR_ID:}
    private-key-path: ${CLOUDFRONT_PRIVATE_KEY_PATH:}

# CDN 설정 (개발)
cdn:
  enabled: ${CDN_ENABLED:false}
  base-url: ${CDN_BASE_URL:}
  image-path: /images
  static-path: /static
  cache-duration: 86400

---
# 운영 환경 설정 (PostgreSQL)
spring:
  config:
    activate:
      on-profile: prod

  # Redis/Elasticsearch/Mail/Sentry 자동설정 비활성화 (Koyeb 무료 플랜에서 미사용)
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.data.elasticsearch.ElasticsearchDataAutoConfiguration
      - org.springframework.boot.autoconfigure.data.elasticsearch.ElasticsearchRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.elasticsearch.ElasticsearchRestClientAutoConfiguration
      - org.springframework.boot.autoconfigure.elasticsearch.ElasticsearchClientAutoConfiguration
      - org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration
      - org.springframework.boot.autoconfigure.mail.MailSenderValidatorAutoConfiguration
      - io.sentry.spring.boot.jakarta.SentryAutoConfiguration

  # Spring Boot 시작 최적화
  main:
    lazy-initialization: true
    banner-mode: off

  jmx:
    enabled: false

  # JPA 초기화 지연
  data:
    jpa:
      repositories:
        bootstrap-mode: deferred

  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:travelmate}?sslmode=prefer}
    username: ${DB_USERNAME:travelmate_user}
    # SECURITY: 운영환경에서 DB_PASSWORD 환경변수 필수 설정
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 120000
      connection-timeout: 30000
      max-lifetime: 1800000
      validation-timeout: 5000
    continue-on-error: false
      
  jpa:
    hibernate:
      ddl-auto: update  # 스키마 자동 업데이트
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          batch_size: 100
        order_inserts: true
        order_updates: true
        cache:
          use_second_level_cache: false
          use_query_cache: false
            
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=10m
      
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: 0
    jedis:
      pool:
        max-active: 10
        max-idle: 5
        min-idle: 1

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
      probes:
        enabled: false
    metrics:
      enabled: false
    prometheus:
      enabled: false
  health:
    redis:
      enabled: false
    elasticsearch:
      enabled: false
    mail:
      enabled: false
    db:
      enabled: false

logging:
  level:
    root: WARN
    com.travelmate: WARN
    org.springframework: WARN
    org.hibernate: ERROR
    org.apache: ERROR
    com.zaxxer.hikari: WARN
  pattern:
    console: '%d{HH:mm:ss} %-5level %logger{20} - %msg%n'

app:
  jwt:
    secret: ${JWT_SECRET}  # 필수! 운영환경에서 반드시 설정 (최소 32자)
    expiration: ${JWT_EXPIRATION:3600000}  # 1 hour
    refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}  # 7 days

  mail:
    enabled: ${MAIL_ENABLED:true}  # 운영환경에서는 활성화

  redis:
    enabled: false  # Koyeb 무료 플랜에서 Redis 미사용

  frontend:
    url: ${FRONTEND_URL:https://travelmate.app}

  file:
    upload:
      path: ${FILE_UPLOAD_PATH:/app/uploads/}
      max-size: 5242880  # 5MB in bytes

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://various-belva-untab-1a59bee2.koyeb.app,https://travelmate.app,https://admin.travelmate.app}

  security:
    rate-limit:
      enabled: true
      requests-per-minute: 60
      auth-requests-per-minute: 5
    xss:
      enabled: true
    audit:
      enabled: true
    headers:
      # SECURITY: CSP 정책 강화 - unsafe-eval 제거, unsafe-inline은 style에만 허용 (CSS-in-JS 지원)
      # 프론트엔드에서 nonce 기반 스크립트 로딩으로 전환 권장
      content-security-policy: "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' https:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"

# AWS 설정 (운영)
aws:
  region: ${AWS_REGION:ap-northeast-2}
  access-key-id: ${AWS_ACCESS_KEY_ID:}
  secret-access-key: ${AWS_SECRET_ACCESS_KEY:}
  use-default-credentials: false
  cloudfront:
    distribution-id: ${CLOUDFRONT_DISTRIBUTION_ID:}
    key-pair-id: ${CLOUDFRONT_KEY_PAIR_ID:}
    private-key-path: ${CLOUDFRONT_PRIVATE_KEY_PATH:}

# CDN 설정 (운영)
cdn:
  enabled: ${CDN_ENABLED:true}
  base-url: ${CDN_BASE_URL:https://cdn.travelmate.app}
  image-path: /images
  static-path: /static
  cache-duration: 604800

# Sentry 설정 (운영) - 선택적 설정
sentry:
  enabled: ${SENTRY_ENABLED:false}
  dsn: ${SENTRY_DSN:}
  environment: production
  traces-sample-rate: ${SENTRY_TRACES_SAMPLE_RATE:0.2}
  logging:
    minimum-event-level: error
    minimum-breadcrumb-level: warn
  send-default-pii: false

---
# 테스트 환경 설정
spring:
  config:
    activate:
      on-profile: test
      
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
    
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        
logging:
  level:
    com.travelmate: WARN
    org.springframework: WARN
    org.hibernate: WARN
    
app:
  jwt:
    secret: testSecretKey123
    expiration: 3600000