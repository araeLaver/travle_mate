# Play Console 앱 콘텐츠 선언 — 답안

두리메이트(`com.doorimate.app`) 기준. **코드에서 확인한 사실**만 적었고, 근거 파일을 함께 표기했다.
경로: Play Console → 두리메이트 → `/app-content/overview`
(좌측 네비게이션은 클릭해도 라우팅되지 않으니 이 URL로 직접 갈 것)

## 완료된 것 (2026-09-19)

| 항목 | 답변 |
|---|---|
| 개인정보처리방침 | `https://fryndo-web.vercel.app/legal` |
| 광고 | 아니요, 앱에 광고가 없습니다 |
| 정부 앱 | 아니요 |
| 건강 | 앱에 건강 기능이 없음 |

---

## 1. 로그인 세부정보 (`/app-content/testing-credentials`)

**앱에 제한된 부분이 있나요? → 예**

로그인 없이는 인증 화면 외에 아무것도 볼 수 없다.

| 필드 | 값 |
|---|---|
| 이름 | 전체 앱 |
| 사용자 이름 | `qa-check-20260913@example.com` |
| 비밀번호 | (아래 참고) |

비밀번호는 이 계정을 만들 때 쓴 값이다. 라이브 서버(`fryndo-untab-f93a6aa7.koyeb.app`)에서
로그인 200을 확인했다. 계정이 살아있는지 다시 확인하려면:

```bash
curl -s -o /dev/null -w "%{http_code}\n" --max-time 120 \
  -X POST https://fryndo-untab-f93a6aa7.koyeb.app/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"qa-check-20260913@example.com","password":"<비밀번호>"}'
```

**기타 안내란에 반드시 적을 것:**

> 백엔드가 무료 호스팅이라 일정 시간 요청이 없으면 절전 상태가 됩니다.
> 첫 로그인 요청은 최대 60초까지 걸릴 수 있으니 기다려 주세요. 이후 요청은 1~2초입니다.

이걸 안 적으면 "앱이 응답하지 않는다"로 반려될 수 있다.

---

## 2. 타겟층 및 콘텐츠

**연령대 → 18세 이상만 선택**

낯선 사람과 만나 함께 여행하는 동행 매칭 앱이다. 미성년자를 대상으로 하지 않는다.
18세 미만을 하나라도 선택하면 Families 정책이 적용되어 요구사항이 크게 늘어난다.

- **아동의 관심을 끌 수 있나요? → 아니요** (아동 대상 캐릭터·게임 요소 없음)
- 스토어 등록정보에 아동 대상 문구 없음

---

## 3. 콘텐츠 등급

시작 시 **이메일 주소**를 묻는다 → `kdowndan@gmail.com`
**카테고리 → 소셜 네트워크 / 커뮤니케이션**

설문 답변(전부 근거 있음):

| 질문 | 답 | 근거 |
|---|---|---|
| 폭력·유혈 | 아니요 | 해당 콘텐츠 없음 |
| 성적 콘텐츠 | 아니요 | 없음 |
| 욕설 | 아니요 | 앱이 제공하는 콘텐츠에 없음 |
| 마약·주류·담배 | 아니요 | 없음 |
| 도박 | 아니요 | 없음 |
| 공포·무서운 요소 | 아니요 | 없음 |
| **사용자 간 상호작용/소통** | **예** | 그룹 채팅 (`ChatRoomScreen`, `/chat/rooms/*`) |
| **사용자가 위치를 공유할 수 있나요** | **예** | 채팅에서 위치 전송 (`handleSendLocation`), 주변 사용자 매칭 |
| **사용자 생성 콘텐츠를 공유** | **예** | 채팅 메시지, 프로필 자기소개, 프로필 사진 |
| 개인정보를 제3자와 공유 | 아니요 | 외부 공유 없음 |
| 디지털 콘텐츠 구매 | 아니요 | IAP는 `src/lib/iapShim.ts` — 결제 불가 껍데기 |

> 사용자 간 소통·위치 공유가 있으므로 등급이 올라가는 게 정상이다. 숨기면 안 된다.

---

## 4. 데이터 보안 (가장 김)

### 수집·공유 여부

- **수집하나요 → 예**
- **제3자와 공유하나요 → 아니요**
  (Sentry DSN은 플레이스홀더라 비활성, 모바일에 광고·분석 SDK 없음)

### 수집 항목

| 데이터 | 수집 | 필수/선택 | 목적 | 근거 |
|---|---|---|---|---|
| 이메일 주소 | 예 | 필수 | 계정 관리 | 회원가입·로그인 |
| 이름(닉네임) | 예 | 필수 | 계정 관리, 앱 기능 | 회원가입 |
| 비밀번호 | 예 | 필수 | 계정 관리 | BCrypt 해시 저장 |
| 사용자 ID | 예 | 필수 | 계정 관리 | 내부 id |
| **대략적 위치** | 예 | 선택 | 앱 기능(주변 장소·동행 추천) | `getDeviceLocation`, `/nft/nearby` |
| **정확한 위치** | 예 | 선택 | 앱 기능 | `ACCESS_FINE_LOCATION` |
| 사진 | 예 | 선택 | 앱 기능(프로필 사진) | `expo-image-picker` |
| **앱 내 메시지** | 예 | 선택 | 앱 기능(그룹 채팅) | `chat_messages` |
| 기타 사용자 생성 콘텐츠 | 예 | 선택 | 앱 기능(자기소개) | 프로필 편집 |
| 기기 ID | 예 | 필수 | 앱 기능(세션 관리) | refresh token의 deviceId |
| 나이·성별 | 예 | 선택 | 앱 기능(동행 추천) | 프로필 편집 |

**수집하지 않는 것** (체크하지 말 것): 결제 정보, 주소, 전화번호(입력란은 있으나 선택),
주소록, 통화 기록, SMS, 캘린더, 설치된 앱 목록, 검색 기록, 웹 방문 기록, 피트니스·건강 정보.

### 보안 관행

| 질문 | 답 | 근거 |
|---|---|---|
| 전송 중 암호화 | **예** | 모든 API가 HTTPS |
| 데이터 삭제 요청 가능 | **예** | 앱 내 `나 → 계정 삭제` (2026-09-19 추가), 백엔드 `DELETE /users/account` |
| 독립적 보안 검토 | 아니요 | 받은 적 없음 |

> ⚠️ **계정 삭제는 방금 추가한 기능이라 현재 내부 테스트에 올라간 vc6에는 없다.**
> 이 선언을 내기 전에 **반드시 재빌드해서 올릴 것.** 없는 기능을 있다고 선언하면 안 된다.

---

## 5. 금융 기능

**아무것도 체크하지 않음.**

특히 **"토큰화된 디지털 애셋(NFT) 판매·거래·보상"을 체크하지 말 것.** 근거:

- 앱에 지갑·민팅 버튼·마켓플레이스·경매 화면이 없다 (NFT 관련 화면은 `CollectionScreen` 하나)
- `nftService.requestMinting()`은 어느 화면에서도 호출되지 않는 죽은 코드
- 백엔드의 `MarketplaceController`·`AuctionController`를 앱이 한 번도 부르지 않는다
- 프로덕션은 블록체인 비활성 (`BLOCKCHAIN_REQUIRE_IN_PROD=false`, 기동 로그 "온체인 민팅 불가")
- 포인트는 구매도 현금화도 불가능한 인앱 숫자

2026-09-19에 앱 UI에서 "NFT·민팅" 표현도 걷어냈다(커밋 `b8ea811`).
**나중에 온체인 민팅을 켜면 이 선언을 다시 해야 한다.**

---

## 남은 순서

1. 위 선언 5건 입력
2. 스토어 등록정보 — 설명은 `travelmate-mobile/store/metadata.json`, 스크린샷 8장과
   아이콘·피처 그래픽은 `travelmate-mobile/store/` 에 있다
3. **계정 삭제·문구 수정이 들어간 빌드로 재빌드 후 업로드**
4. 비공개 테스트 시작 + **테스터 12명** → 여기서 14일 시계가 돈다
5. 14일 경과 후 프로덕션 액세스 신청

프로덕션까지는 어떤 경우에도 **오늘부터 최소 14일**이다. 4번을 가장 먼저 시작할수록 빨라진다.
