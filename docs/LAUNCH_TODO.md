# 출시까지 남은 일 — 당신이 해야 할 것

2026-09-19 기준. 위에서부터 순서대로 하면 된다.
**프로덕션 출시일 = 테스터 12명이 모인 날 + 14일.** 그래서 ③번을 가장 먼저 시작하는 게 제일 빠르다.

---

## ① 스토어 등록정보 — 스크린샷만 올리면 끝 (10분)

**바로가기**: Play Console → 두리메이트 →
`https://play.google.com/console/u/0/developers/8227560958218591979/app/4973108778878021518/main-store-listing`

앱 이름·설명·아이콘·피처 그래픽은 **이미 다 들어가 있다**(설명은 임시보관함에 저장해둠).
비어 있는 건 **휴대전화 스크린샷** 하나뿐이다.

**올릴 파일** — 이 폴더에서 드래그:

```
travelmate-mobile/store/screenshots/android/phone_play/
```

> 원본(1080×2400)은 비율이 1:2.22라 Play가 거부한다(허용 최대 1:2).
> `phone_play/` 안의 파일은 9:16(1350×2400)으로 패딩해둔 것이라 그대로 통과한다. **`phone/` 폴더 말고 `phone_play/`를 쓸 것.**

**어떤 걸 올릴지**

- 지금 바로 올릴 수 있는 7장(문구 깨끗함): `01_login_light` `02_login_dark` `03_register` `06_chat_list` `07_chat_room` `09_matching` `10_map`
- `04_home` `05_collection` `08_profile` 3장은 **옛 빌드라 "수집 NFT" 문구가 남아 있다.** 새 빌드로 다시 찍어야 한다(내가 할 수 있음, 30분)

7장만 올려도 요건(2~8장)은 충족된다. 다 올렸으면 **저장**.

---

## ② 앱 콘텐츠 선언 — 6건 남음

**바로가기**: `.../app/4973108778878021518/app-content/overview`

> 좌측 메뉴는 눌러도 안 넘어간다. 위 URL로 직접 갈 것.

내가 5건(개인정보처리방침·광고·정부 앱·건강·금융 기능)을 끝냈다. 남은 6건은 아래대로.

### 2-1. 위치 정보 액세스 권한 / 포그라운드 서비스 권한 — **재빌드하면 사라짐**

이 두 건은 앱이 안 쓰는 권한을 매니페스트에 선언해서 생긴 것이다.
방금 `app.json`에서 그 권한들을 지웠으므로 **새 빌드를 올리면 이 선언 2건은 목록에서 없어진다.**
지금 답하지 말고 재빌드 후에 확인할 것. (재빌드는 내가 함)

### 2-2. 타겟층 및 콘텐츠

- 연령대 → **18세 이상만** 체크
- 아동의 관심을 끌 수 있나요 → **아니요**

> 18세 미만을 하나라도 고르면 Families 정책이 걸려 요구사항이 확 늘어난다.

### 2-3. 콘텐츠 등급

- 이메일 → `kdowndan@gmail.com`
- 카테고리 → **소셜 네트워크 / 커뮤니케이션**

| 질문 | 답 |
|---|---|
| 폭력·유혈 / 성적 콘텐츠 / 욕설 / 마약·주류·담배 / 도박 / 공포 | 전부 **아니요** |
| 사용자 간 상호작용·소통 | **예** (그룹 채팅) |
| 사용자가 위치를 공유할 수 있음 | **예** (채팅 위치 전송) |
| 사용자 생성 콘텐츠 공유 | **예** (채팅·자기소개·프로필 사진) |
| 개인정보를 제3자와 공유 | **아니요** |
| 디지털 콘텐츠 구매 | **아니요** |

> 소통·위치 공유가 있으니 등급이 올라가는 게 정상이다. 숨기면 나중에 문제가 된다.

### 2-4. 데이터 보안

- 수집하나요 → **예** / 제3자와 공유하나요 → **아니요**

| 데이터 | 필수/선택 | 목적 |
|---|---|---|
| 이메일 주소 | 필수 | 계정 관리 |
| 이름(닉네임) | 필수 | 계정 관리, 앱 기능 |
| 비밀번호 | 필수 | 계정 관리 |
| 사용자 ID | 필수 | 계정 관리 |
| 기기 ID | 필수 | 앱 기능(세션) |
| 대략적 위치 | 선택 | 앱 기능(주변 장소·동행 추천) |
| 정확한 위치 | 선택 | 앱 기능 |
| 사진 | 선택 | 앱 기능(프로필 사진) |
| 앱 내 메시지 | 선택 | 앱 기능(그룹 채팅) |
| 기타 사용자 생성 콘텐츠 | 선택 | 앱 기능(자기소개) |
| 나이·성별 | 선택 | 앱 기능(동행 추천) |

**체크하지 말 것**: 결제 정보, 주소, 주소록, 통화 기록, SMS, 캘린더, 설치된 앱, 검색 기록, 건강 정보

보안 관행:
- 전송 중 암호화 → **예**
- 데이터 삭제 요청 가능 → **예** ← 앱에 계정 삭제를 방금 넣었다. **단, 그 기능이 들어간 빌드를 올린 뒤에 답할 것**
- 독립적 보안 검토 → **아니요**

### 2-5. 로그인 세부정보 ← **비밀번호 입력이 필요해 내가 못 한 것**

- 앱에 제한된 부분이 있나요 → **예**
- 이름: `전체 앱`
- 사용자 이름: `qa-check-20260913@example.com`
- 비밀번호: 이 계정 만들 때 쓴 값 (모르면 나에게 물어볼 것)

**기타 안내란에 반드시 넣을 것:**

> 백엔드가 무료 호스팅이라 일정 시간 요청이 없으면 절전됩니다.
> 첫 로그인은 최대 60초까지 걸릴 수 있으니 기다려 주세요. 이후에는 1~2초입니다.

이걸 안 적으면 "앱이 응답하지 않음"으로 반려될 수 있다.

---

## ③ 테스터 12명 — **이게 출시일을 결정한다. 지금 시작할 것**

- 개인 개발자 계정은 프로덕션 액세스 신청 전에 **12명 이상 × 14일 이상 비공개 테스트**가 필요하다
- **내부 테스트는 이 14일에 산입되지 않는다** (지금 올라가 있는 vc7은 내부 테스트)
- 필요한 건 **지메일 계정 12개**. 지인이든 본인 부계정이든 상관없다
- 넣는 곳: `테스트 및 출시 → 테스트 → 비공개 테스트 → 테스터`

①②를 아무리 빨리 끝내도 이게 없으면 시계가 안 돈다.

---

## ④ 그 다음 (자동으로 따라오는 것)

1. 비공개 테스트 트랙에 빌드 출시 → 14일 대기
2. 14일 경과 + 테스터 12명 유지 → **프로덕션 액세스 신청**
3. 구글 심사 → 프로덕션 출시

---

## 내가 이어서 할 수 있는 것

- 권한 정리가 반영된 **재빌드 + 업로드** (선언 2건이 사라지고, 데이터 보안의 "삭제 가능=예" 근거가 생긴다)
- 스크린샷 3장(홈·컬렉션·프로필) **재촬영**
- 남은 선언 중 비밀번호가 필요 없는 것들 추가 시도

필요한 걸 말하면 바로 한다.
